[project]
name = "l2-cwa"
version = "0.1.0"
description = "Taiwan Weather Forecast"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.31.0",
    "pandas>=2.0.0",
    "python-dotenv>=1.0.0"
]

[tool.vercel]
entrypoint = "api.index:handler"
